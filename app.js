// Funkcija za otvaranje popup prozora
function openPopup() {
    popup.style.display = 'block';
    displayImage(currentImageIndex);
    console.log('popup hello'); // Dodajte ovu liniju za ispis u konzolu
}
